import styled from "styled-components";

const Main = styled.ul`
    font-size: 1.4rem;
    padding: 1rem;
    font-weight: 400;

    li {
        margin: .2rem 0;
    }

    p {
        text-align: center;
    }
`;

export default Main;